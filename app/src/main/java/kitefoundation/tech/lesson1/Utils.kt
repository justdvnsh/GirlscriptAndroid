package kitefoundation.tech.lesson1

import java.util.*
import kotlin.collections.HashMap

class Utils {
    companion object {

        fun trimString(str: String) = str.trim()

        fun convertToUppercase(str: String) = str.toUpperCase(Locale.ROOT)
    }
}